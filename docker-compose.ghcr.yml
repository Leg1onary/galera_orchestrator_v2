# ============================================================
# Galera Orchestrator v2 — deploy via GHCR
# Используй этот файл если НЕ хочешь собирать образ локально.
# Образ берётся из GitHub Container Registry (собирается автоматически CI).
#
# Использование:
#   cp .env.example .env && nano .env
#   docker compose -f docker-compose.ghcr.yml up -d
#
# Обновление до последней версии:
#   docker compose -f docker-compose.ghcr.yml pull
#   docker compose -f docker-compose.ghcr.yml up -d
# ============================================================

services:
  orchestrator:
    image: ghcr.io/leg1onary/galera_orchestrator_v2:latest
    container_name: galera-orchestrator
    restart: unless-stopped

    # env_file передаёт значения без shell-интерполяции,
    # что критично для bcrypt-хешей с символами $
    env_file:
      - .env

    # Переменные без $ можно оставить здесь для явности,
    # они будут перезаписаны env_file в случае конфликта
    environment:
      SSH_KEY_PATH: /root/.ssh/id_rsa

    ports:
      - "${HOST_PORT:-8000}:8000"

    volumes:
      - orchestrator-data:/data
      - ${SSH_KEY_HOST_PATH:-~/.ssh/id_rsa}:/root/.ssh/id_rsa:ro

    healthcheck:
      test: >
        curl -sf -o /dev/null -w "%{http_code}"
        http://localhost:8000/api/auth/me | grep -qE "^(200|401)$$"
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3

    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

volumes:
  orchestrator-data:
    driver: local
